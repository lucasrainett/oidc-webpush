# oidc-webpush — Feature & Assumption Confirmation

> **Do not edit this file in-place.**
> Respond to each item with **YES**, **NO**, or a **NOTE**.
> We will not write code until every item below is resolved.

---

## 1. Project Fundamentals

| #   | Item                                                                                                                                                                                                | Status                                                            | Example Scenario                                                                                                                                                                                                                                                                                                             | Scenario Reply                                                                                                         |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| 1.1 | **Project name is `oidc-webpush`** (not `notify-app`). All user-facing strings, comments, file headers, and package metadata must be updated.                                                       | yes                                                               | A user opens the web UI and sees the browser tab title "oidc-webpush", the package.json name field reads "oidc-webpush", and the systemd service file generated by install.sh is named `oidc-webpush.service`.                                                                                                               | yes, topic done                                                                                                        |
| 1.2 | **Scope**: Self-hosted email → Web Push gateway. One binary (Node.js). No external mail relay required.                                                                                             | yes                                                               | A Proxmox backup job sends an SMTP message to `alice@example.com` on port 2525. The app receives it directly, matches it to Alice, and pushes it to her phone. No Postfix, Mailrise, or Sendgrid is involved.                                                                                                                | yes, topic done                                                                                                        |
| 1.3 | **No frontend build step**. `public/` is served as static files. No bundler, no React/Vue.                                                                                                          | maybe, I do prefer all code in typescript                         | The browser loads `index.html`, `style.css`, `client.js`, and `sw.js` exactly as they sit on disk. No `vite build`, no `webpack`, no transpilation. If we want TypeScript safety for frontend logic, we compile `src/public/*.ts` → `public/*.js` as a separate build step, but the runtime still serves plain static files. | lets create the code in typescript, and a git hook to build, so the the final js is also in the repository, topic done |
| 1.4 | **Single-file backend** (`src/app.ts`) is acceptable until the file exceeds ~800 lines. Only then do we split into modules.                                                                         | no, this has chenged, it is ok to have multiple files             | The backend is organized as `src/routes.ts`, `src/db.ts`, `src/smtp.ts`, `src/push.ts` from day one. This keeps concerns separated and makes testing/review easier.                                                                                                                                                          | yes, topic done                                                                                                        |
| 1.5 | **SQLite only**. No PostgreSQL, no Redis. `better-sqlite3` with WAL mode. One database file.                                                                                                        | yes for now, make the code to allow changing this                 | The app ships with `DATABASE_URL=./data/oidc-webpush.db` by default. All DB code is wrapped in a thin repository layer so that swapping in PostgreSQL later only requires changing the driver, not every SQL query.                                                                                                          | yes, topic done (use funtional code not OO)                                                                            |
| 1.6 | **No formal migration framework** for now. Schema changes use `CREATE TABLE IF NOT EXISTS` and `ALTER TABLE ADD COLUMN`. A `schema_version` table is added only when migrations become unavoidable. | yes, but add a version in current db so that we can use in future | On first startup the app creates tables and inserts `INSERT INTO schema_version (version) VALUES (1)`. In six months when we add a `rules.origin` column, the app checks `schema_version`, sees `1`, runs `ALTER TABLE`, then updates to `2`. No third-party migration library.                                              | yes, topic done                                                                                                        |

---

## 2. Authentication & Identity

| #   | Item                                                                                                                                                                            | Status                                                                     | Example Scenario                                                                                                                                                                                                                                                                                                                                                                                 | Scenario Reply  |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------- |
| 2.1 | **SSO via Authentik OIDC only**. No local user registration, no username/password, no API keys for browser access.                                                              | SSO via OIDC only, no need to mention authentik, any spec oidc should work | Alice clicks "Sign In" and is redirected to her company's Keycloak instance. After login she is redirected back to the app. The app works exactly the same if the provider is Authentik, Keycloak, Auth0, or Dex, as long as it supports standard OIDC Discovery, `code` response type, and PKCE.                                                                                                | yes, topic done |
| 2.2 | **PKCE flow** with server-side session storage in SQLite (`sessions` table). Session cookie is `sid`, `HttpOnly`, `SameSite=Lax`, 7-day expiry.                                 | not sure, explain                                                          | Alice logs in via her OIDC provider. The app stores a random session ID in the `sessions` table with `expires_at = now + 7 days`. The browser gets a cookie `sid=abc123` that JavaScript cannot read (`HttpOnly`). On every request the app looks up `sid` in the DB. After 7 days the row is deleted and Alice must log in again.                                                               | yes, topic done |
| 2.3 | **No CSRF token middleware**. Relying on `SameSite=Lax` + OIDC state/nonce for auth routes.                                                                                     | not sure, explain                                                          | When Alice clicks "Sign In", the app generates a random `state` and `nonce` and stores them in a short-lived cookie. The OIDC provider returns the same `state` in the callback. The app compares them. For regular dashboard actions (delete device, create rule), the browser sends the `sid` cookie automatically; `SameSite=Lax` prevents cross-site requests from triggering these actions. | yes, topic done |
| 2.4 | **User identity is derived from Authentik claims**: `sub` (primary key), `email`, `name`/`preferred_username` → `display_name`. No editable user profile fields stored locally. | yes                                                                        | Alice changes her display name in the SSO provider from "Alice Smith" to "Ali". She signs out and back into the app. The app updates `users.display_name` to "Ali" automatically. There is no "Edit Profile" button inside the app itself.                                                                                                                                                       | yes, topic done |
| 2.5 | **No Traefik `forwardAuth`**. The app handles its own OIDC redirect and callback.                                                                                               | yes                                                                        | Traefik only terminates TLS and forwards requests to `http://10.0.0.5:3000`. The app itself sends the 302 redirect to the OIDC provider and handles the `/auth/callback` route. There is no `forwardAuth` middleware in Traefik pointing back to Authentik.                                                                                                                                      | yes, topic done |
| 2.6 | **Session cleanup** runs every hour (in-process timer). Expired rows are deleted from `sessions`. No need for a cron job or external scheduler.                                 | not sure, explain                                                          | The Node.js process sets a `setInterval` that runs every 60 minutes and executes `DELETE FROM sessions WHERE expires_at < now()`. This runs inside the single app process. If the process restarts, the timer restarts. No systemd timer, no cron, no Redis TTL.                                                                                                                                 | yes, topic done |

---

## 3. SMTP Ingress

| #   | Item                                                                                                                                                                                                                      | Status                                                                                | Example Scenario                                                                                                                                                                                                                                                                                                                                                                                             | Scenario Reply  |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------- |
| 3.1 | **Built-in SMTP server** (`smtp-server` on port 2525). No Mailrise, no Postfix, no external relay.                                                                                                                        | yes                                                                                   | A Proxmox backup job is configured with `SMTP host: oidc-webpush.local`, `Port: 2525`. It opens a TCP socket directly to the Node.js process, sends `MAIL FROM` and `RCPT TO`, and the app handles the rest internally.                                                                                                                                                                                      | yes, topic done |
| 3.2 | **Plain text SMTP, no STARTTLS**. Trust boundary is the network (VLAN 30). TLS support is explicitly deferred until the SMTP port is exposed beyond the trusted network.                                                  | trust is the netwokr, no need to mention vlan, this will be a MIT open source project | The SMTP socket listens on port 2525 and accepts `EHLO` / `MAIL FROM` / `RCPT TO` / `DATA` without ever sending a `220` with `STARTTLS` capability. This is acceptable because the app runs on a trusted LAN or inside a Kubernetes cluster where traffic is already encrypted at the network layer (WireGuard, Cilium, etc.).                                                                               | yes, topic done |
| 3.3 | **Optional SMTP authentication** via `SMTP_AUTH_USER` / `SMTP_AUTH_PASS`. If unset, anyone on the network can submit mail.                                                                                                | no, admin users should create smtp credentials to each application using it           | Alice opens the dashboard, goes to "Apps", clicks "New Credential", names it "Proxmox Backup". The app generates a username `cred_aB3xK9` and a secret token `sk-xxxxxxxx`. Alice pastes these into Proxmox. Proxmox authenticates with `AUTH PLAIN` on port 2525. The app looks up the credential and routes the mail to Alice.                                                                             | yes, topic done |
| 3.4 | **Routing is by recipient address only** (for now). Exact email match first, then local-part fallback (`alice@any.domain` matches user whose email local part is `alice`). No per-app credential routing in v0.1.         | no, only full lowercase email matching, if no user found, log it for admin review     | An SMTP message arrives with `RCPT TO: <Alice@Example.COM>`. The app lowercases it to `alice@example.com`, queries `SELECT * FROM users WHERE LOWER(email) = 'alice@example.com'`. If no row matches, the event is logged with `status='no_user'` and an admin can review unmatched events in the dashboard. No local-part fallback.                                                                         | yes, topic done |
| 3.5 | **Unauthenticated mail to an unknown recipient** logs a `no_user` event and is silently dropped. No bounce message is sent back to the sender.                                                                            | yes                                                                                   | An IoT device sends mail to `bob@example.com` but Bob has never logged in. The app writes a row to `events` with `status='no_user'` and returns SMTP code `250 OK` to the sender. The sender thinks the mail was accepted. Bob is never notified because there is no user to route to.                                                                                                                       | yes, topic done |
| 3.6 | **SMTP `onData` parses the entire message with `mailparser`**, then iterates `rcptTo`. If one recipient fails, the others still get processed. The SMTP callback only fails on parse errors, not routing/delivery errors. | not sure, explain                                                                     | A script sends one email with `RCPT TO: <alice@example.com>` and `RCPT TO: <bob@example.com>`. The stream is parsed once into a `ParsedMail` object. For Alice, the app finds her devices and sends pushes. For Bob, the app finds no user and logs `no_user`. The SMTP transaction returns `250 OK` because parsing succeeded. If the stream were malformed, the app would return `554 Transaction failed`. | yes, topic done |

---

## 4. Web Push & Delivery

| #   | Item                                                                                                                                                                                    | Status                                                                                                                           | Example Scenario                                                                                                                                                                                                                                                                                                                                                                                                      | Scenario Reply                                                                                   |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| 4.1 | **VAPID-authenticated Web Push** (`web-push` library). Public/private keys generated once and treated as permanent (rotation invalidates all subscriptions).                            | 2 options, admin can provide their own keys, or auto genereate, if auto generate show it one time so that admin can save it      | On first startup with no `VAPID_PUBLIC`/`VAPID_PRIVATE` env vars, the app generates a key pair and prints them to stdout with a banner: `=== SAVE THESE KEYS ===`. The admin copies them into `.env`. If the admin already has keys (e.g. from another instance), they paste them into `.env` and the app uses them directly.                                                                                         | yes, topic done                                                                                  |
| 4.2 | **Push subscriptions are per-user, per-device**. Stored as `endpoint`, `p256dh`, `auth`. Upsert on re-subscription (update `last_seen` and `user_agent`).                               | not sure, explain                                                                                                                | Alice opens the app on her iPhone and clicks "Enable on this device". The browser calls `pushManager.subscribe()`, returning a unique endpoint `https://fcm.googleapis.com/fcm/send/abc...`. The app stores this under Alice's user ID. A month later Alice reinstalls her PWA. The browser returns the same endpoint. The app updates `last_seen = now()` instead of creating a duplicate row.                       | not sure, you mean she install in the same browser same device?                                  |
| 4.3 | **Dead subscriptions are auto-pruned** on 404/410 from the push provider. No manual sweep required for v0.1.                                                                            | not sure, explain                                                                                                                | Alice uninstalls the PWA from her phone. The push provider (FCM) returns `410 Gone` when the app tries to send her a notification. The app catches this error and immediately runs `DELETE FROM subscriptions WHERE endpoint = '...'`. The next time Alice opens the app on another device, she sees only active subscriptions listed.                                                                                | yes, topic done                                                                                  |
| 4.4 | **Push payload limit (~4 KB)** is accepted. Email body is truncated to 200 chars for the push body. No "tap to view full message" deep-link into the app (no message body persistence). | no, admin can provide ollama api to create a AI summary of the message, ai can also decide to not send email based on importance | A monitoring system sends a 50 KB log dump. Instead of truncating to 200 chars, the app sends the `subject` + first 4000 chars of `body` to Ollama. Ollama returns: `"summary": "Disk full on host db-01", "relevant": true`. The push body becomes "Disk full on host db-01". If Ollama returns `"relevant": false`, the push is suppressed and an `ai_suppressed` event is logged.                                  | yes, but I am ok to send a lot of data to ollama, truncate but in a very high amount, topic done |
| 4.5 | **Notification display** is handled entirely by the Service Worker. The server sends JSON payload; the SW calls `self.registration.showNotification`.                                   | yes, or what other options we have?                                                                                              | The server POSTs a Web Push message containing JSON: `{"title":"Disk full","body":"db-01 at 98%"}`. The Service Worker receives the `push` event, parses the JSON, and calls `showNotification("Disk full", { body: "db-01 at 98%", icon: "/icons/icon-192.png" })`. The browser renders the OS-native notification bubble. There is no other way to display a Web Push notification; the SW is the only entry point. | yes, topic done                                                                                  |
| 4.6 | **Service Worker** handles `push`, `notificationclick` (focus/open window), and `pushsubscriptionchange` (auto-resubscribe + POST to `/api/subscribe`).                                 | not sure, explain                                                                                                                | (1) A push arrives: SW shows the notification. (2) Alice taps the notification: SW calls `clients.openWindow('/')` or focuses an existing tab. (3) Alice's browser rotates the push subscription endpoint silently: SW calls `pushManager.subscribe()` with the VAPID key and POSTs the new endpoint to `/api/subscribe` so the server stays in sync.                                                                 | I still didn't get it.                                                                           |
| 4.7 | **Priority levels**: `1-2` → low urgency, `3` → default, `4-5` → high (requireInteraction + vibrate). Determined by rules engine (`priority` action) or default 3.                      | yes, also defined by AI                                                                                                          | Alice has a rule: `subject ~ /CRITICAL/ → priority 5`. A monitoring email with subject `[CRITICAL] DB down` arrives. The push payload carries `priority: 5`. The Service Worker sets `requireInteraction: true` and `vibrate: [200, 100, 200]`. If AI is enabled and the model returns `"priority": 4`, that value overrides the default but a user rule still takes precedence over AI.                              | yes, topic done                                                                                  |

---

## 5. Rules Engine

| #   | Item                                                                                                                                                   | Status                                                         | Example Scenario                                                                                                                                                                                                                                                                                                                                                         | Scenario Reply                                              |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------- |
| 5.1 | **Per-user rules only** (no global/system-wide rules in v0.1).                                                                                         | yes                                                            | Alice creates a rule `from ~ /nagios@example.com/ → mute`. This only suppresses Nagios emails for Alice. Bob still receives them unless he creates his own mute rule. There is no admin panel to create a rule that applies to every user simultaneously.                                                                                                                | yes, topic done                                             |
| 5.2 | **Match fields**: `from`, `subject`, `body`.                                                                                                           | yes                                                            | A rule can match the sender address (`from`), the email subject line (`subject`), or the plain-text body (`body`). There is no match on `to` address, headers, or attachment names in v0.1.                                                                                                                                                                              | yes, topic done                                             |
| 5.3 | **Match pattern** is treated as RegEx if valid, otherwise falls back to case-insensitive substring contains.                                           | yes                                                            | Alice enters pattern `/backup (ok success)/i`. The app compiles it as a RegExp and matches "backup ok". If Alice enters `backup ok` (no slashes), the app does `body.toLowerCase().includes('backup ok')`. She never needs to know the difference in the UI.                                                                                                             | yes, topic done                                             |
| 5.4 | **Actions**: `mute` (drop silently), `priority` (value is numeric string, default 3), `tag` (value is string label).                                   | what is tag?                                                   | A `tag` action adds a label to the push notification (e.g. `tag: "work"`). In the Service Worker, this appears as `options.tag = "work"`, which groups notifications in the OS tray. Two pushes with the same tag replace each other instead of stacking. Alice could tag all CI/CD emails with `tag: "ci"` so only the latest build status shows.                       | yes, topic done                                             |
| 5.5 | **Rules are evaluated in `position` order** (ascending). First match wins. No chaining, no compound conditions (`AND`/`OR`).                           | yes                                                            | Alice has three rules: (1) `from ~ /boss/ → priority 5`, (2) `subject ~ /lunch/ → mute`, (3) `from ~ /hr/ → tag work`. An email from `boss@example.com` with subject "lunch meeting" hits rule 1 first and gets priority 5. Rule 2 is never checked for this message. There is no way to say "from /boss/ AND subject /lunch/".                                          | yes, for now, topic done                                    |
| 5.6 | **Rules have no `origin` column yet** (noted in backlog for dismiss-action feature). v0.1 rules are indistinguishable from later auto-generated rules. | I didn't get you                                               | In v0.1, every rule in the `rules` table was created by a user clicking "+ add rule" in the dashboard. In a future version, a user might tap "Mute this" on a notification, which auto-creates a rule. Without an `origin` column, the dashboard cannot show which rules were hand-written vs auto-generated. We are deciding whether to add `origin TEXT` now or later. | the user should be able to pick the "app name" for the rule |
| 5.7 | **No rule enable/disable toggle** (no `enabled` column). To disable, delete the rule.                                                                  | enable/disable is a good feature, do it if make things simpler | Alice creates a rule to mute `newsletter@example.com` during vacation. Instead of deleting it when she returns, she clicks a toggle to re-enable it. The `rules` table has `enabled INTEGER DEFAULT 1`. Disabled rules are skipped during evaluation but remain in the DB so she can turn them back on.                                                                  | yes, topic done                                             |

---

## 6. Frontend & UI

| #   | Item                                                                                                                                                             | Status                     | Example Scenario                                                                                                                                                                                                                                                                                                                                                   | Scenario Reply                                                                                |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------- |
| 6.1 | **htmx 2.x from CDN** (`unpkg.com`). Not vendored into `public/` for v0.1.                                                                                       | no, no external dependency | The app serves `public/htmx.min.js` from its own disk. No fetch to `unpkg.com`, no reliance on external uptime. The admin can air-gap the server and the dashboard still works.                                                                                                                                                                                    | how about only vanillar javascript?                                                           |
| 6.2 | **UI is a single-page dashboard** with three sections: Devices, Rules, Events. No navigation, no routing, no separate pages.                                     | yes                        | Alice opens `/` and sees a top bar with her email and a sign-out button. Below that, three stacked cards: "Devices" (with remove buttons), "Rules" (with add form), and "Events" (with test push button). There are no URLs like `/rules` or `/settings`; everything is on `/`.                                                                                    | yes, topic done                                                                               |
| 6.3 | **Events list refreshes automatically** every 15 seconds (`hx-trigger="load, every 15s"`).                                                                       | no, manual                 | Alice must click the "refresh" button next to the Events header to see new events. The events list does not poll the server automatically. This reduces load and avoids distracting UI updates while she is reading.                                                                                                                                               | yes, topic done                                                                               |
| 6.4 | **All dynamic content is HTML fragments** returned by API routes (`text/html`). No JSON API for the dashboard.                                                   | not sure, explain          | When Alice clicks "refresh" on Events, the browser sends `GET /api/events`. The server returns raw HTML: `<div class="row">...</div><div class="row">...</div>`. htmx swaps this directly into the DOM. The browser never parses JSON or builds DOM nodes from data. The only JSON endpoints are `/api/me`, `/api/vapid-public-key`, and the Service Worker fetch. | lets use json, and for events send the last seen event, so that the server only send new ones |
| 6.5 | **PWA manifest and service worker are present**, but no actual icon files (`/icons/icon-192.png`, `/icons/icon-512.png`, `/icons/badge-72.png`) are shipped yet. | we will find a logo later  | The `manifest.json` references `/icons/icon-192.png`. If the file is missing, Chrome shows a generic grey icon when installing the PWA. We will generate or source an SVG logo and export PNGs before the first release, but the code works without them.                                                                                                          | yes, topic done                                                                               |
| 6.6 | **No dark/light mode toggle**. Single dark theme (`#0e0f12` background, amber accent).                                                                           | no theme in v0.1           | The CSS defines exactly one color scheme: dark background, amber buttons, green success, red error. There is no "☀️ / 🌙" toggle, no `prefers-color-scheme` media query, no CSS variables for light mode.                                                                                                                                                          | yes, topic done                                                                               |
| 6.7 | **No mobile-native app**. PWA "Add to Home Screen" is the only mobile path.                                                                                      | yes, only PWA              | Alice opens Safari on her iPhone, navigates to the app, taps Share → "Add to Home Screen". It installs as a fullscreen PWA with push support via the Service Worker. There is no Swift/Kotlin app in the App Store.                                                                                                                                                | yes, topic done                                                                               |

---

## 7. Data Model (Current)

| #   | Item                                                                                                                                                                | Status                                                        | Example Scenario                                                                                                                                                                                                                                                                                                                                                                                                                  | Scenario Reply                                                                                |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- |
| 7.1 | **`users`**: `sub` PK, `email` UNIQUE, `display_name`, `created_at`. No `is_admin` column.                                                                          | no, is_admin is needed as an admin can make other users admin | Bob is the first user. The `ADMIN_EMAILS` env var lists `bob@example.com`, so on his first login `is_admin = 1` is set. Bob opens the user list, sees Alice, and clicks "Make admin". The app runs `UPDATE users SET is_admin = 1 WHERE sub = 'alice-sub'`. Alice now sees admin UI elements on next refresh.                                                                                                                     | yes, topic done                                                                               |
| 7.2 | **`subscriptions`**: `id` PK, `user_sub` FK, `endpoint` UNIQUE, `p256dh`, `auth`, `user_agent`, `created_at`, `last_seen`.                                          | yes, if accompish the business features                       | Alice has an iPhone and a laptop. The `subscriptions` table holds two rows for her `user_sub`: one with her iPhone's FCM endpoint and one with her laptop's Mozilla autopush endpoint. When she logs in from a new device, a third row is added.                                                                                                                                                                                  | yes, and in future version user can pick what device get what message, topic done             |
| 7.3 | **`rules`**: `id` PK, `user_sub` FK, `position`, `match_field`, `match_pattern`, `action`, `action_value`, `created_at`.                                            | yes, if accompish the business features                       | Alice has two rules: (1) `position=0, match_field='from', match_pattern='nagios', action='mute'`, (2) `position=1, match_field='subject', match_pattern='/urgent/', action='priority', action_value='5'`. The `action_value` is `NULL` for the mute rule because mute doesn't need a value.                                                                                                                                       | yes, topic done                                                                               |
| 7.4 | **`events`**: `id` AUTOINCREMENT, `ts`, `user_sub`, `from_addr`, `to_addr`, `subject`, `matched_rule`, `action_taken`, `delivered_count`, `failed_count`, `status`. | yes, if accompish the business features                       | A backup job sends mail to Alice. The app logs: `status='delivered', delivered_count=2, failed_count=0` (two devices got it). Another mail to an unknown user logs: `status='no_user', user_sub=NULL, delivered_count=0`. An AI-suppressed mail logs: `status='ai_suppressed', action_taken='ai_skip'`.                                                                                                                           | yes, topic done                                                                               |
| 7.5 | **`sessions`**: `sid` PK, `user_sub`, `created_at`, `expires_at`.                                                                                                   | yes, if accompish the business features                       | Alice logs in on Monday. A row is inserted with `sid='uuid-abc', expires_at=Monday+7days`. She stays logged in all week. The following Monday, the cleanup timer deletes the expired row. Her next click redirects her to the OIDC login page.                                                                                                                                                                                    | yes, the time should respect the token time,  topic done                                      |
| 7.6 | **No `schema_version` table** for now.                                                                                                                              | no                                                            | On first startup the app creates `schema_version (version INTEGER PRIMARY KEY, applied_at INTEGER)`. It inserts `version=1`. Six months later we add the `rules.origin` column: the app checks `SELECT MAX(version) FROM schema_version`, sees `1`, runs `ALTER TABLE`, then inserts `version=2`.                                                                                                                                 | yes, topic done                                                                               |
| 7.7 | **No user preference table** (e.g. for AI opt-in/out, timezone, theme).                                                                                             | not sure                                                      | Alice wants to turn off AI filtering. Without a `user_prefs` table, we would have to add columns to `users` (`use_ai_filter`, `use_ai_summary`). A dedicated `user_prefs (user_sub, key, value)` table is more flexible for future settings (timezone, notification sound, etc.).                                                                                                                                                 | yes, topic done                                                                               |
| 7.8 | **Events do not persist full email bodies**. Only `from`, `to`, `subject` are stored.                                                                               | not sure, what are the implications?                          | A monitoring email with a 2 MB stack trace arrives. The app stores only `from_addr='alert@prometheus.local'`, `to_addr='alice@example.com'`, `subject='Firing: HighMemoryUsage'`. The 2 MB body is discarded after parsing and push delivery. Implications: (a) Alice cannot click an event in the dashboard to read the full message, (b) disk usage stays low, (c) if the push is missed, the original content is gone forever. | yes, but be sure the AI summary has the link to the tool that created the message, topic done |

---

## 8. Feature Backlog — Admin Role

| #   | Item                                                                                                                                                | Status                                                                                                       | Example Scenario                                                                                                                                                                                                                                                                                                                                                                        | Scenario Reply                                                                              |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- |
| 8.1 | **Admin status is derived from `ADMIN_EMAILS` env var** (comma-separated, case-insensitive). No DB column. No UI for granting/revoking admin in v1. | no, use env var for default admins, but other users can be made admins later, env admins cannot be turned of | `ADMIN_EMAILS=bob@example.com` is set. Bob logs in and `users.is_admin` is automatically set to `1`. He cannot be demoted from the UI. Later, Bob promotes Alice to admin via the UI. Alice's `is_admin` flips to `1`. Alice can be demoted by Bob. Bob (env admin) is permanent.                                                                                                       | yes, topic done                                                                             |
| 8.2 | **Admin views are read-only in v1**: Users list, System stats, Unmatched events. No mutations (no delete user, no edit rules on behalf of others).  | no, admins can edit everything                                                                               | Bob opens the admin panel, sees Alice's rules, and deletes a noisy mute rule she created by accident. He also views the "Unmatched events" page and sees that an app is sending to `alic@example.com` (typo). He adds a user alias or contacts the app owner.                                                                                                                           | no user alias, if is wrong fix is in the sso, the email comes from there                    |
| 8.3 | **Admin UI is a separate section/tab**, toggled via `.admin-only` CSS class based on `is_admin` from `/api/me`.                                     | no, same use, but with more options                                                                          | The dashboard is still one page with Devices, Rules, Events. If `is_admin` is true, additional sections appear inline: "All Users", "System Stats", "Unmatched Events". There is no separate `/admin` URL; the extra content is conditionally rendered on `/`.                                                                                                                          | admin can select a user, and see the interface as that user, to do anything the user can do |
| 8.4 | **Future**: DB-backed admin grants (`users.is_admin` or `admin_grants` table) overlaid on env-set. Env-listed admins are permanent recovery path.   | I didn't unserstand you                                                                                      | There are two ways an admin gets power: (1) Their email is in `ADMIN_EMAILS` env var — this is the "break glass" recovery path if the DB is corrupted. (2) Another admin clicked "Make admin" on them — this is stored in `users.is_admin = 1`. The effective admin check is `is_env_admin OR is_db_admin = 1`. Env admins are always admins; DB admins can be toggled by other admins. | yes, topic done                                                                             |

---

## 9. Feature Backlog — Ollama AI Integration

| #   | Item                                                                                                                                         | Status                                                       | Example Scenario                                                                                                                                                                                                                                                                                                                                     | Scenario Reply                                        |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------- |
| 9.1 | **Local Ollama instance** (e.g. `http://workstation:11434`, `llama3.2:3b`). No cloud LLM.                                                    | yes                                                          | The app sends an HTTP POST to `http://ollama.local:11434/api/generate` with `model: "llama3.2:3b"`. No API key, no OpenAI, no Claude. The model runs on the admin's own GPU (RTX 4070 Ti). If Ollama is unreachable, the app falls back to non-AI behavior.                                                                                          | yes, topic done                                       |
| 9.2 | **AI does two things**: (a) relevance filtering (`relevant: true/false`), (b) one-sentence summary (max 140 chars).                          | yes, maybe in future admin can update the AI prompt          | A CI/CD email arrives with body "Build #4521 failed on stage test-e2e...". The app POSTs to Ollama with a prompt template. The model returns `{"relevant": true, "summary": "Build 4521 failed at E2E tests"}`. The push body is "Build 4521 failed at E2E tests". In future, the admin might edit the prompt template in `.env` or a settings file. | yes, topic done                                       |
| 9.3 | **AI is never a hard gate on delivery**. Timeout, parse failure, or Ollama down → fall back to current behavior (truncated body, send).      | yes, or send only email subject without content              | Ollama is offline. The app tries to connect, times out after 5 seconds, catches the error, and falls back to sending a push with `title = email.subject` and `body = (email.text ?? '').slice(0, 200)`. The user still gets notified; the only difference is the body is a raw snippet instead of an AI summary.                                     | yes, topic done                                       |
| 9.4 | **Per-user opt-in for AI filtering** (`use_ai_filter`, default OFF). AI summary is opt-out (`use_ai_summary`, default ON).                   | default ON                                                   | Every new user has `use_ai_filter = 1` and `use_ai_summary = 1` by default. Alice can open a settings panel and turn off "AI filtering" if she wants to receive every email as a push. She can also turn off "AI summary" to get raw body snippets instead.                                                                                          | make this configurable in install script              |
| 9.5 | **Deterministic bypass patterns** (`AI_BYPASS_PATTERNS` env var) override AI verdict. Protects 2FA/OTP/alert keywords from false negatives.  | yes                                                          | `AI_BYPASS_PATTERNS=2fa verification                                                                                                                                                                                                                                                                                                                 | yes, could be multiple values, topic done             |
| 9.6 | **Concurrency limit** (`OLLAMA_MAX_CONCURRENCY`, e.g. 2) via in-process semaphore to avoid starving other GPU users (Paperless, Open WebUI). | yes, and message should queue for processing up to 2 minutes | Ten emails arrive simultaneously. The app allows only 2 concurrent Ollama requests. The other 8 are queued in memory. If an email waits longer than 2 minutes, it is dequeued and falls back to non-AI delivery. The GPU is shared politely with Paperless-GPT and Open WebUI.                                                                       | yes, topid done                                       |
| 9.7 | **AI suppressions are logged** with `status='ai_suppressed'` and visible in the user's events list.                                          | yes                                                          | A marketing email arrives. Ollama returns `"relevant": false`. The app does not send a push, but inserts an event row with `status='ai_suppressed', action_taken='ai_skip', subject='Summer sale!"`. Alice sees this in her Events panel and thinks "Good, AI is working."                                                                           | yes, and in future user can force to send, topic done |
| 9.8 | **No message body is sent to Ollama if it matches `AI_SKIP_PATTERNS`** (sensitive content regex bypass).                                     | yes                                                          | `AI_SKIP_PATTERNS=password reset                                                                                                                                                                                                                                                                                                                     | yes, topic done                                       |

---

## 10. Feature Backlog — In-Notification "Mute This"

| #    | Item                                                                                                                                                | Status                                     | Example Scenario                                                                                                                                                                                                                                                                                                                                                                       | Scenario Reply                                               |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| 10.1 | **Notification action button** (`mute-type`) on Android/Desktop. iOS fallback via deep-link (`/event/<id>?action=mute`).                            | yes                                        | Alice receives a push from `newsletter@example.com` on her Android phone. The notification shows two buttons: "Open" and "Mute this". She taps "Mute this". The Service Worker POSTs to `/api/mute` with the sender address. A new rule is created. On iOS, the notification has no buttons; tapping it opens the browser to `/event/123?action=mute` which shows a confirmation page. | how ios users trigger the same action as "open" for android? |
| 10.2 | **Dismissal creates a `mute` rule** in the user's ruleset, scoped to the `from` address (`from ~ /^<exact-sender>$/i`).                             | no, dismiss is normal should not auto mute | Alice swipes away the notification from `newsletter@example.com`. The notification disappears from her tray. Nothing else happens. No rule is created. If she wants to mute future emails, she must open the app and add a rule manually (or use the action button if available).                                                                                                      | yes, topic done                                              |
| 10.3 | **Rules table gets `origin` column** (`user`, `dismiss_action`, `ai_learned`). Dismissals are indistinguishable from manual rules except by origin. | not sure, explain                          | Alice has three mute rules. One she created manually (`origin='user'`). One was created by tapping "Mute this" on a notification (`origin='dismiss_action'`). One was suggested by AI and she confirmed it (`origin='ai_learned'`). In the dashboard she can filter to see only auto-generated rules and clean them up. Without the column, all three look identical.                  | v0.1 no ai suggestion                                        |
| 10.4 | **Undo is manual**: user opens app → Rules section → deletes the auto-created rule. No "undo" notification action.                                  | yes                                        | Alice accidentally taps "Mute this" on a 2FA email. She opens the app, goes to Rules, sees a new rule `from ~ /auth@example.com/ → mute` with origin `dismiss_action`, and clicks delete. She does not get a "Undo mute" button on the notification itself.                                                                                                                            | yes, topic done                                              |
| 10.5 | **No time-limited mutes in v1** (no `expires_at` on rules). Permanent only.                                                                         | not sure, explain                          | Alice mutes `newsletter@example.com`. The rule stays forever until she deletes it. There is no "Mute for 30 days" option. In v2 we might add `rules.expires_at` so that a noisy maintenance window auto-expires.                                                                                                                                                                       | yes, topic done                                              |
| 10.6 | **Dismiss action on one device affects all devices** (server-side rule). Already-delivered notifications on other devices are not recalled.         | yes                                        | Alice taps "Mute this" on her phone. The server creates the rule. Her laptop, which also has the PWA installed, will not receive future emails from that sender. However, a notification that was already sitting in her laptop's tray before she muted remains there. Web Push has no "recall" primitive.                                                                             | yes, topic done                                              |

---

## 11. Feature Backlog — Per-App SMTP Credentials

| #    | Item                                                                                                                                                                                                        | Status            | Example Scenario                                                                                                                                                                                                                                                                                                                                                                                                   | Scenario Reply                                                                                                                                                                                   |
| ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 11.1 | **Users can create named app credentials** ("Proxmox", "Immich"). Username = credential ID (`nanoid`), password = random secret token.                                                                      | yes               | Alice clicks "New App Credential", names it "Proxmox". The app generates username `cred_aB3xK9` and a random secret `sk-xxxxxxxxxxxxxxxx`. She copies both into Proxmox's SMTP settings. The credential row is linked to her `user_sub`.                                                                                                                                                                           | yes, topic done                                                                                                                                                                                  |
| 11.2 | **Passwords stored as `argon2` hash** (`@node-rs/argon2` or `argon2`).                                                                                                                                      | yes               | The secret `sk-xxxxxxxx` is hashed with Argon2id and stored as `$argon2id$v=19$m=65536,t=3,p=4$...`. When Proxmox authenticates, the app hashes the provided password and compares it with the stored hash. If the DB is leaked, the original secrets are computationally expensive to crack.                                                                                                                      | yes, topic done                                                                                                                                                                                  |
| 11.3 | **Routing precedence**: (1) Authenticated credential → credential's owner. (2) Unauthenticated → recipient-address routing (current). (3) Optional `SMTP_REQUIRE_AUTH=1` disables unauthenticated fallback. | not sure, explain | Proxmox connects and sends `AUTH PLAIN cred_aB3xK9 sk-xxxxxxxx`. The app looks up the credential, finds it belongs to Alice, and routes the email to Alice regardless of the `RCPT TO` address. If an old script sends unauthenticated mail to `alice@example.com`, the app falls back to recipient routing. If `SMTP_REQUIRE_AUTH=1` is set, unauthenticated mail is rejected with `535 Authentication required`. | no, the credential is related to proxmox sender, so the app know the message was from proxmox, the receiver is found from the receiver of the email, also no "unauthenticatied" option available |
| 11.4 | **Credentials can be toggled on/off** (`enabled` column). Revoke = delete row.                                                                                                                              | yes               | Alice goes on vacation and doesn't want Proxmox alerts. She clicks the toggle next to "Proxmox" to disable the credential. Proxmox's next backup attempt gets `535 Authentication failed`. She re-enables it when she returns. If she deletes the credential, the username `cred_aB3xK9` is gone forever and Proxmox must be reconfigured with a new one.                                                          | if alice is a normal user, she can't see anything related to credentials                                                                                                                         |
| 11.5 | **Per-credential stats**: `message_count`, `last_used_at`, `error_count` (if any).                                                                                                                          | yes               | Alice sees: "Proxmox — 42 messages, last used 2h ago, 0 errors". She sees another credential "Old Immich — 3 messages, last used 90d ago, 5 errors". She deletes the old one because it's unused and error-prone.                                                                                                                                                                                                  | if alice is a normal user, she can't see anything related to credentials                                                                                                                         |
| 11.6 | **Token shown once on creation** (like API key). No plaintext retrieval after creation.                                                                                                                     | yes               | When Alice clicks "Create", a modal shows: `Username: cred_aB3xK9Password: sk-xxxxxxxx` with a "Copy" button. After she closes the modal, the password is never shown again. If she loses it, she must rotate (delete + recreate) the credential.                                                                                                                                                                  | if alice is a normal user, she can't see anything related to credentials                                                                                                                         |

---

## 12. Deployment & Operations

| #    | Item                                                                                                                                 | Status                             | Example Scenario                                                                                                                                                                                                                                                                     | Scenario Reply                                                                               |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------- |
| 12.1 | **Primary install path**: Proxmox VE unprivileged LXC via `install.sh`. One-line `bash -c "$(curl ...)"`.                            | no, install show work on any linux | A user on a plain Ubuntu 24.04 server clones the repo, runs `./install.sh`, and it installs Node 24, builds the app, generates VAPID keys, creates a systemd unit, and starts the service. The same script works on Debian, Fedora, or Arch without assuming Proxmox `pct` commands. | no manual repo clone, provide single line bash intaller similar to proxmox community scripts |
| 12.2 | **Docker is supported** but secondary. Dockerfile is multi-stage, non-root user (`uid 1001`), healthcheck on `/healthz`.             | no need docker at this time        | There is no `Dockerfile` in the repo for now. If someone wants containerization later, we will add it. The focus is on bare-metal / VM / LXC deployment.                                                                                                                             | user can run the single line isnstall script in a docker contariner if they want             |
| 12.3 | **Node.js >=20 required**. Current Dockerfile uses Node 22.                                                                          | node 24 LTS                        | The `package.json` engines field says `"node": ">=24"`. CI runs on Node 24. The install script installs Node 24.x LTS via Nodesource or n. Node 20 is not supported.                                                                                                                 | yes, topic done                                                                              |
| 12.4 | **No systemd unit is shipped in the repo** — `install.sh` generates it on the target LXC.                                            | yes                                | The repo contains no `oidc-webpush.service` file. When `install.sh` runs on the target machine, it writes `/etc/systemd/system/oidc-webpush.service` with the correct `WorkingDirectory`, `User`, and `EnvironmentFile` paths for that machine.                                      | yes, topic done                                                                              |
| 12.5 | **No reverse proxy config is shipped** (Traefik example is in README only).                                                          | yes                                | The README contains a Traefik YAML snippet for reference, but no `traefik/` directory or `docker-compose.yml` is in the repo. Users bring their own reverse proxy (Nginx, Caddy, Traefik, cloud LB).                                                                                 | no need, user environments are to different to assume anything                               |
| 12.6 | **Logs go to stdout/stderr** (Fastify logger). No log file rotation is the app's responsibility.                                     | yes                                | The app prints JSON log lines to stdout. systemd captures them to the journal (`journalctl -u oidc-webpush`). The admin configures `journald` retention or pipes to `logrotate`. The app never writes to `./logs/app.log`.                                                           | yes, topic done                                                                              |
| 12.7 | **No metrics endpoint** (no Prometheus, no `/metrics`).                                                                              | yes                                | There is no `/metrics` route returning Prometheus-format stats. Monitoring is done via log scraping or external health checks on `/healthz`. Future versions might add OpenTelemetry, but not in v0.1.                                                                               | yes, topic done                                                                              |
| 12.8 | **No backup/restore utility** for the SQLite database. Admin is expected to back up `data/oidc-webpush.db` and WAL files externally. | yes                                | The admin runs `rsync /opt/oidc-webpush/data/ /backup/oidc-webpush/` via their own cron. The app does not have a "Export DB" button or a built-in S3 upload. Restoring is copying the file back and restarting the service.                                                          | don't mention anything about backup                                                          |

---

## 13. Deferred / Explicitly Not Doing

| #     | Item                                                                                                                 | Status                            | Example Scenario                                                                                                                                                                                                                                                                                                     | Scenario Reply                                                                 |
| ----- | -------------------------------------------------------------------------------------------------------------------- | --------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| 13.1  | **Mailrise integration** — redundant, app speaks SMTP natively.                                                      | delete this, it will never happen | Mailrise is a separate tool that translates SMTP to webhooks. We will never integrate it because `oidc-webpush` is itself an SMTP server. This item is removed from all docs and notes.                                                                                                                              | yes, topic done                                                                |
| 13.2  | **STARTTLS on SMTP** — deferred until SMTP is exposed beyond the trusted network.                                    | yes delay                         | The SMTP server on port 2525 does not advertise `STARTTLS`. This is fine for a LAN or VPN. If a user later wants to expose port 2525 to the public internet, they must wrap it with `stunnel`, `socat`, or a cloudflare tunnel. We will not add native STARTTLS in v0.1.                                             | yes, topic done                                                                |
| 13.3  | **React/Vue/SPA frontend** — htmx is sufficient.                                                                     | still considering                 | We are keeping htmx for v0.1. If interactivity grows beyond fragments (e.g. real-time charts, complex rule builder), we may revisit a lightweight framework like Preact or Svelte. For now, vanilla JS + htmx is the path.                                                                                           | ok                                                                             |
| 13.4  | **Formal migration framework** — `CREATE IF NOT EXISTS` is enough through v0.3.                                      | yes                               | We add a `schema_version` table now (see 7.6) but do not use a library like `node-pg-migrate` or `umzug`. Migrations are hand-written SQL blocks guarded by `if (currentVersion < 2) { db.exec('ALTER TABLE ...'); db.exec('INSERT INTO schema_version ...') }`.                                                     | yes, topic done                                                                |
| 13.5  | **PostgreSQL/Redis** — SQLite only.                                                                                  | yes                               | All data lives in a single `.db` file on disk. There is no `pg` dependency, no Redis client, no connection pool. The app opens the SQLite file with WAL mode on startup and keeps one connection handle for the lifetime of the process.                                                                             | yes, topic done                                                                |
| 13.6  | **Email body persistence / full-message viewer** — not needed until payload limits become painful.                   | not sure, explain                 | After an email is processed, its full body is discarded. Alice cannot click an event in the dashboard to read the original 2 MB log dump. She must rely on the push notification or check the original sender's logs. If we later add body persistence, we would need a `messages` table and a `/message/:id` route. | yes, topic done                                                                |
| 13.7  | **Broadcast push to all users** — deferred (noted in backlog, not v0.1).                                             | yes                               | An admin wants to send "System maintenance tonight" to every enrolled device. There is no "Broadcast" button in v0.1. Workaround: create a synthetic user with a shared email address and have apps send to that address.                                                                                            | yes, topic done                                                                |
| 13.8  | **Group/shared topics** (e.g. "admin alerts") — deferred. Decide synthetic user vs groups before per-app creds land. | not sure, explain                 | Three admins want to receive the same CrowdSec alert. Option A: create a fake user `admin@example.com` and have CrowdSec send there. Option B: add a `groups` table and allow users to subscribe to topics. We have not decided which approach to take, so we defer both until per-app creds are stable.             | no in v0.1, multi message require multi email, or email to multiple recipients |
| 13.9  | **Per-rule enable/disable toggle** — deferred. Delete to disable.                                                    | yes                               | Wait — the user said `enable/disable is a good feature, do it if make things simpler` in 5.7. This item in the Deferred section contradicts that. We will implement `rules.enabled` now and remove this deferred item.                                                                                               | I am confused by this                                                          |
| 13.10 | **Rule `expires_at` / time-limited mutes** — deferred to v2 of dismiss feature.                                      | yes                               | Alice mutes `newsletter@example.com` and the rule stays forever. There is no "Mute for 7 days" option. If she wants to re-enable later, she must delete the rule and recreate it. We may add `expires_at` after the dismiss feature is stable.                                                                       | yes, topic done                                                                |

---

## How to Respond

For each item, replace the empty `|` cell with one of:

- `YES` — confirmed, do not change.
- `NO` — reject this assumption; note what should happen instead.
- `NOTE: <your note>` — mostly yes, but with a caveat or clarification.

Example:

```
| 1.1 | ... | NOTE: keep "oidc-webpush" internally, but user-facing name should stay "notify" |
```

Once every row is filled, commit this file and we will begin implementation in order of priority.
