# oidc-webpush — Open Items for Discussion

> Items extracted from `CONFIRM.md` where the scenario was not fully accepted or requires further clarification.
> I have added my interpretation/response for each. Reply in the **Owner Reply** column with YES / NO / NOTE.

---

## 4. Web Push & Delivery

| #   | Item                                                                                                                                                    | Original Scenario                                                                                                                                                                                                                                                                               | Your Previous Reply                                             | Assistant Reply                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Owner Reply     |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- |
| 4.2 | **Push subscriptions are per-user, per-device**. Upsert on re-subscription (update `last_seen` and `user_agent`).                                       | Alice opens the app on her iPhone and clicks "Enable on this device". The browser calls `pushManager.subscribe()`, returning a unique endpoint. A month later Alice reinstalls her PWA. The browser returns the same endpoint. The app updates `last_seen` instead of creating a duplicate row. | not sure, you mean she install in the same browser same device? | **Yes, same browser + same device.** Each browser (Safari, Chrome) generates a unique endpoint per origin. If Alice uninstalls and reinstalls the PWA on the same iPhone in Safari, she gets the same endpoint. If she opens Chrome on the same phone, she gets a *different* endpoint (second row). If she opens Safari on her laptop, she gets a third endpoint. Upsert by endpoint prevents duplicates.                                                                                                                                                                                                    | yes, topic done |
| 4.6 | **Service Worker** handles `push`, `notificationclick` (focus/open window), and `pushsubscriptionchange` (auto-resubscribe + POST to `/api/subscribe`). | (1) A push arrives: SW shows the notification. (2) Alice taps the notification: SW opens or focuses the app tab. (3) Alice's browser silently rotates the push subscription endpoint: SW resubscribes and POSTs the new endpoint to `/api/subscribe` so the server stays in sync.               | I still didn't get it.                                          | **Think of the Service Worker as a background daemon that lives in the browser even when the web page is closed.** Three jobs: (a) When the push provider (FCM/Mozilla) delivers a message, the SW wakes up and calls `showNotification()` to draw the bubble. (b) When Alice taps the bubble, the SW decides whether to open a new browser tab or focus an existing one. (c) If the push provider decides to rotate her endpoint (rare, but happens), the SW detects this and sends the new endpoint to our server so we don't lose the ability to push to her. Without a SW, Web Push does not work at all. | yes, topic done |

---

## 5. Rules Engine

| #   | Item                                                                               | Original Scenario                                                                                                                                                                                                                 | Your Previous Reply                                         | Assistant Reply                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Owner Reply                                                                                              |
| --- | ---------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| 5.6 | **Rules table `origin` column** for distinguishing manual vs auto-generated rules. | In v0.1, every rule is created by clicking "+ add rule". In future, a user might tap "Mute this" on a notification, which auto-creates a rule. Without `origin`, the dashboard cannot distinguish hand-written vs auto-generated. | the user should be able to pick the "app name" for the rule | **You want a free-text `app_name` column on rules?** Example: When Alice creates a rule, she can type "Proxmox" in an "App name" field. The rule row stores `app_name='Proxmox'`. Later, when she creates a mute from a notification, the SW sends the sender address and we can optionally tag it with an app name if we can infer it. This is separate from `origin` (which tracks *how* the rule was created: user/dismiss/ai). So we add both: `origin TEXT` (how it was created) and `app_name TEXT` (user-defined label). Does that match what you meant? | the user can pick from a combobox the name of the previouslly created credentials (created by the admin) |

---

## 6. Frontend & UI

| #   | Item                                                                                                           | Original Scenario                                                                                                                                                                                                                                       | Your Previous Reply                                                                           | Assistant Reply                                                                                                                                                                                                                                                                                                                                                                        | Owner Reply     |
| --- | -------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- |
| 6.1 | **htmx vendored into `public/` instead of CDN**.                                                               | The app serves `public/htmx.min.js` from its own disk. No external fetch. The admin can air-gap the server and the dashboard still works.                                                                                                               | how about only vanillar javascript?                                                           | **Drop htmx entirely and write vanilla JS.** The frontend uses `fetch()` to call APIs and manually updates `innerHTML` or creates DOM nodes. No external library, no build step for the frontend. This means ~50-100 extra lines of JS in `client.js` to handle the swaps ourselves. The `public/` folder contains only our own code.                                                  | yes, topic done |
| 6.4 | **All dynamic content is HTML fragments** returned by API routes (`text/html`). No JSON API for the dashboard. | When Alice clicks "refresh" on Events, the browser sends `GET /api/events`. The server returns raw HTML fragments. htmx swaps this directly into the DOM. The only JSON endpoints are `/api/me`, `/api/vapid-public-key`, and the Service Worker fetch. | lets use json, and for events send the last seen event, so that the server only send new ones | **All dashboard endpoints return JSON.** The client fetches JSON, builds DOM nodes in JS, and appends them. For events: the client remembers the highest `event.id` it has seen. On refresh, it sends `GET /api/events?after=42`. The server returns only events with `id > 42`. The client appends them to the list instead of replacing the whole thing. No HTML fragments anywhere. | yes, topic done |

---

## 8. Admin Role

| #   | Item                                              | Original Scenario                                                                                                                                   | Your Previous Reply                                                                         | Assistant Reply                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Owner Reply     |
| --- | ------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- |
| 8.2 | **Admin views: unmatched events page**.           | Bob views "Unmatched events" and sees that an app is sending to `alic@example.com` (typo). He adds a user alias or contacts the app owner.          | no user alias, if is wrong fix is in the sso, the email comes from there                    | **Admin sees unmatched events as read-only debugging info.** The admin cannot create aliases or fix emails inside the app. If `alic@example.com` is a typo, the admin sees it in the unmatched list, then goes to the SSO provider (Authentik/Keycloak) to fix the user's email, or fixes the sender app's config. The app is not an email management system.                                                                                                                                      | yes, topic done |
| 8.3 | **Admin UI is a separate section/tab** vs inline. | The dashboard is one page with Devices, Rules, Events. If `is_admin` is true, additional sections appear inline. There is no separate `/admin` URL. | admin can select a user, and see the interface as that user, to do anything the user can do | **Admin impersonation / "act as user" feature.** The admin sees a "Switch to user" dropdown at the top of the page. Selecting "Alice" reloads the dashboard showing Alice's devices, rules, and events. The admin can delete Alice's device, create a rule for Alice, or send a test push to Alice's devices. A banner reads "You are viewing as Alice [Return to admin view]". No separate page; the same `/` URL changes content based on an `?as_user=alice_sub` query param or a session flag. | yes, topic done |

---

## 9. Ollama AI Integration

| #   | Item                                                                                                                   | Original Scenario                                                                                                                                                                      | Your Previous Reply                      | Assistant Reply                                                                                                                                                                                                                                                                                                                                             | Owner Reply     |
| --- | ---------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- |
| 9.4 | **Per-user opt-in for AI filtering** (`use_ai_filter`, default ON). AI summary opt-out (`use_ai_summary`, default ON). | Every new user has `use_ai_filter = 1` and `use_ai_summary = 1` by default. Alice can open a settings panel and turn off "AI filtering" if she wants to receive every email as a push. | make this configurable in install script | **Install script asks:** `Enable AI filtering by default for new users? [Y/n]`. If the admin answers `n`, the `.env` gets `AI_FILTER_DEFAULT=false` and every new user is created with `use_ai_filter = 0`. They must manually opt-in via settings. The `use_ai_summary` remains default ON regardless because it is harmless (just changes the push body). | yes, topic done |

---

## 10. In-Notification "Mute This"

| #    | Item                                                                                         | Original Scenario                                                                                                                                                                                                                | Your Previous Reply                                          | Assistant Reply                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Owner Reply                |
| ---- | -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------- |
| 10.1 | **Notification action button** (`mute-type`) on Android/Desktop. iOS fallback via deep-link. | Alice receives a push on her Android phone. The notification shows two buttons: "Open" and "Mute this". She taps "Mute this". On iOS, the notification has no buttons; tapping it opens the browser to `/event/123?action=mute`. | how ios users trigger the same action as "open" for android? | **iOS users tap the notification body (not a button) to open the app**, which lands on a confirmation page: "Mute all emails from newsletter@example.com? [Confirm] [Cancel]". On Android, the action button is the fast path (skips the confirmation page), but tapping the body also opens the same confirmation page. Both platforms end up at the same mute creation, but iOS requires one extra tap because action buttons are unreliable on iOS PWAs. Is this acceptable? | ok                         |
| 10.3 | **Rules table gets `origin` column** (`user`, `dismiss_action`, `ai_learned`).               | Alice has three mute rules. One she created manually (`origin='user'`). One was created by tapping "Mute this" (`origin='dismiss_action'`). One was suggested by AI and confirmed (`origin='ai_learned'`).                       | v0.1 no ai suggestion                                        | **For v0.1, `origin` values are only `user` and `dismiss_action`.** The `ai_learned` value is reserved for when the AI suggestion feature lands later. The column exists in the schema from day one so we don't need a migration later. Is it okay to add `origin TEXT DEFAULT 'user'` now even though only two values are used in v0.1?                                                                                                                                        | no need the column for now |

---

## 11. Per-App SMTP Credentials

| #         | Item                                                                   | Original Scenario                                                                                                                                                                   | Your Previous Reply                                                                                                                                                                              | Assistant Reply                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Owner Reply     |
| --------- | ---------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- |
| 11.3      | **Routing precedence**: Authenticated credential → credential's owner. | Proxmox connects and sends `AUTH PLAIN cred_aB3xK9 sk-xxxxxxxx`. The app looks up the credential, finds it belongs to Alice, and routes the email to Alice regardless of `RCPT TO`. | no, the credential is related to proxmox sender, so the app know the message was from proxmox, the receiver is found from the receiver of the email, also no "unauthenticatied" option available | **Credential = "this app is authorized to send".** The `RCPT TO` address determines the recipient user (by exact lowercase email match). The credential only proves the sender is legitimate and identifies which app sent it (for stats/labeling). Every SMTP connection MUST authenticate; there is no unauthenticated mode. If `RCPT TO` is unknown, the mail is logged as `no_user` and dropped even if the credential was valid. Does this match your model? | yes, topic done |
| 11.4–11.6 | **Normal users cannot see/manage credentials**.                        | Alice (normal user) sees "Apps" section with credentials, toggle, stats, and token creation.                                                                                        | if alice is a normal user, she can't see anything related to credentials                                                                                                                         | **Only admins manage SMTP credentials.** Normal users see only: Devices, Rules, Events. Admins see an additional "Apps" (or "SMTP Credentials") section where they create named credentials, copy tokens, toggle on/off, and view stats. When an admin creates a credential for an app, they assign it to a specific user (the recipient). Is this correct?                                                                                                       | yes, topic done |

---

## 12. Deployment & Operations

| #    | Item                                                                        | Original Scenario                                                                                                                                                                          | Your Previous Reply                                                                          | Assistant Reply                                                                                                                                                                                                                                                                                                                                                                                                        | Owner Reply                   |
| ---- | --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------- |
| 12.1 | **Primary install path**: Proxmox VE unprivileged LXC via `install.sh`.     | A user on a plain Ubuntu 24.04 server clones the repo, runs `./install.sh`, and it installs Node 24, builds the app, generates VAPID keys, creates a systemd unit, and starts the service. | no manual repo clone, provide single line bash intaller similar to proxmox community scripts | **One-liner:** `bash -c "$(curl -fsSL https://example.com/install.sh)"`. The script downloads itself, detects the OS (Debian/Ubuntu/Fedora/Arch), installs Node 24 via the appropriate method, clones the repo to `/opt/oidc-webpush`, runs `npm ci && npm run build`, generates VAPID keys if missing, writes `.env` interactively, creates a systemd unit, and starts the service. The user never types `git clone`. | yes, but use pnpm, topic done |
| 12.2 | **Docker is supported** but secondary.                                      | There is no `Dockerfile` in the repo for now. The focus is on bare-metal / VM / LXC deployment.                                                                                            | user can run the single line install script in a docker container if they want               | **No Dockerfile or Docker docs in the repo.** If a power user wants to run in Docker, they can use an official `node:24` image and run the same one-liner install script inside it. We don't maintain or test a Docker path for v0.1.                                                                                                                                                                                  | yes, topic done               |
| 12.5 | **No reverse proxy config is shipped** (Traefik example is in README only). | The README contains a Traefik YAML snippet for reference, but no `traefik/` directory or `docker-compose.yml` is in the repo.                                                              | no need, user environments are to different to assume anything                               | **README mentions only that the app listens on port 3000 (HTTP) and 2525 (SMTP).** No sample configs for Nginx, Caddy, or Traefik. No TLS termination guidance. Users are expected to handle their own reverse proxy or expose the app directly if on a trusted network.                                                                                                                                               | yes, topic done               |
| 12.8 | **No backup/restore utility** for the SQLite database.                      | The admin runs `rsync /opt/oidc-webpush/data/ /backup/oidc-webpush/` via their own cron. The app does not have an "Export DB" button.                                                      | don't mention anything about backup                                                          | **No backup functionality, no docs about backing up, no mentions in README or install script.** The SQLite file is in `./data/`. If the admin wants to back it up, that is entirely their responsibility. The app never suggests, automates, or documents backup procedures.                                                                                                                                           | yes, topic done               |

---

## 13. Deferred / Explicitly Not Doing

| #    | Item                                                              | Original Scenario                                                                                                                       | Your Previous Reply                                                            | Assistant Reply                                                                                                                                                                                                                                                                                                             | Owner Reply                 |
| ---- | ----------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------- |
| 13.3 | **React/Vue/SPA frontend** — htmx is sufficient.                  | We are keeping htmx for v0.1. If interactivity grows beyond fragments, we may revisit a lightweight framework.                          | ok                                                                             | **Vanilla JavaScript only.** No htmx, no React, no Vue, no Svelte. We write raw `fetch()` and DOM manipulation in `client.js`. This aligns with your "how about only vanilla javascript?" question in 6.1.                                                                                                                  | I said ok, means topic done |
| 13.8 | **Group/shared topics** (e.g. "admin alerts") — deferred.         | Three admins want to receive the same CrowdSec alert. Option A: create a fake user `admin@example.com`. Option B: add a `groups` table. | no in v0.1, multi message require multi email, or email to multiple recipients | **No groups in v0.1.** To notify multiple people, the sender puts multiple addresses in `RCPT TO`: `RCPT TO: <alice@example.com>`, `RCPT TO: <bob@example.com>`. The app processes each recipient independently and sends pushes to each user's devices. There is no "mailing list" or "group" concept.                     | yes, topic done             |
| 13.9 | **Per-rule enable/disable toggle** — deferred. Delete to disable. | Wait — the user said `enable/disable is a good feature, do it if make things simpler` in 5.7. This deferred item contradicts that.      | I am confused by this                                                          | **There is no contradiction — this deferred item is simply outdated.** In 5.7 you confirmed `rules.enabled` will be implemented. This row in the "Deferred" section was from the original AI-generated notes and is now wrong. We will implement `enabled` toggle now and remove this item from the deferred list entirely. | ok, done                    |

---

## How to Respond

For each item, replace the empty `|` cell in **Owner Reply** with one of:

- `YES` — the Assistant Reply is correct.
- `NO` — reject; explain what should happen instead.
- `NOTE: <your note>` — mostly correct, with a caveat.

Once every row is filled, we will finalize the spec and begin implementation.
